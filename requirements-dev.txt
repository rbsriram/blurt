-r requirements.txt
pytest>=8
# Pinned: the integration suite (docs/test_suite.py) relies on a session-scoped
# event_loop override, which 0.21.x honors directly.
pytest-asyncio==0.21.2
faker>=24
ruff>=0.4
numpy>=1.26
